name: Gemini Workers

on:
  workflow_dispatch: {}     # tetap bisa dijalankan manual
  push:                     # otomatis kalau ada push
    branches:
      - '**'

jobs:
  gemini_worker:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        WORKER_INDEX: [0, 1, 2, 3, 4, 5]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-genai requests

      - name: Jalankan worker Gemini
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}   # diambil dari Secrets
          WORKER_INDEX: ${{ matrix.WORKER_INDEX }}        # index worker
        run: |
          echo "🚀 Start worker index: $WORKER_INDEX"
          python worker_gemini.py
